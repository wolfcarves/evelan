const UserListsTitle = ({ title }: { title: string }) => {
  return (
    <div className="py-4">
      <h1 className="text-center font-mono font-bold text-2xl">{title}</h1>
    </div>
  );
};

export default UserListsTitle;
